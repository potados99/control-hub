#ifdef INCLUDES_h
#define INCLUDES_h

#include <Arduino.h>
#include <SoftwareSerial.h>
#include <errno.h>
#include <string.h>

#define HAS(x, y) ((x) & (y))
#define ADD(x, y) ((x) |= (y))
#define SUB(x, y) ((X) &= ~(Y))

#endif
